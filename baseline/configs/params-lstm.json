{
  "dataset_args": {
    "max_utterances": 20,
    "max_tokens": 512
  },

  "task": "linking",
  "model_name_or_path": "lstm",
  "glove_path": "/root/Fact_Linking/data/glove/glove.6B.300d.txt",

  "vocab_size": 10000,
  "embed_dim": 300,
  "hidden_dim": 300,
  "forward_dim": 1200,
  "lstm_layers": 2,
  "dropout": 0.5,

  "per_gpu_train_batch_size": 8,
  "per_gpu_eval_batch_size": 8,
  "gradient_accumulation_steps": 1,
  
  "learning_rate": 2e-4,
  "adam_epsilon": 1e-8,
  "max_grad_norm": 1.0,

  "num_train_epochs": 20,
  "warmup_steps": 0,

  "fp16": "",

  "seed": 47
}
